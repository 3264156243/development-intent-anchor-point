# Changelog

## [0.3.0] - 2026-08-10

### 修复

- **已安装副本不同步问题**：此前修复发布后，已安装的插件副本仍可能是旧版（旧版生成器生成的
  问卷仍会出现“选一个选项却高亮/勾选同题所有选项”的问题）。请按下方“如何更新”重新安装，确保
  使用的是 `0.3.0`。

### 变更

- **默认多选**：所有问题默认支持多选（可只选一个，也可勾选多个）；需要严格单选的问题在
  `questions.json` 中设置 `"multiple": false` 即可。

### 如何更新

1. 在 Codex 中重新安装插件以拉取最新版本：

   ```bash
   codex plugin add development-intent-anchor-point@development-intent-anchor-point
   ```

2. 安装后**打开一个新任务**再使用（新任务才会加载新技能）；如果仍在使用旧版，重启 Codex 后重试。
3. 重新触发需求确认流程生成新问卷（旧问卷 HTML 文件里内嵌的是旧代码，需要重新生成）。

---

## [0.2.0] - 2026-08-10

### 修复

- **单选高亮 bug**：此前点击任一普通选项时，同一题的所有普通选项都会显示为选中；现在只有真正点选
  的那个选项会高亮，单选/多选状态均正确。

### 新增

- **多选支持**：问题可设为多选（`"multiple": true`，默认），用户可勾选多个选项；`"multiple": false`
  为单选。多选问题会标注「可多选」。「我自己回答 / 不需要 / 后续再定」在两种模式下始终是互斥选项。
- **答案自动保存到问卷同目录**：新增本地服务 `scripts/serve_questionnaire.py`。问卷通过本地
  http 地址打开后，点击「保存答案」会把 `answers.json` 直接写入 `questionnaire.html` 所在目录
  （`.requirements-work/`），不再需要手动下载。若服务未启动，会自动退化为下载 / 复制文本。

### 如何更新

1. 在 Codex 中重新安装插件以拉取最新版本：

   ```bash
   codex plugin add development-intent-anchor-point@development-intent-anchor-point
   ```

2. 安装后**打开一个新任务**再使用（新任务才会加载新技能）；如果仍在使用旧版，重启 Codex 后重试。

---

## [0.1.0] - 2026-08-10

### 初始版本

- 苏格拉底问答式需求确认工作流：项目启动时生成 HTML 问卷并在浏览器打开，用户点选答案后生成
  需求 / 计划 MD 文档，迭代确认直到用户说“开始工作”，随后清理临时文件并依照文档开工；
- 开发过程中遇到未明确的问题，先询问用户而不是自行臆断。
